package pgdp.function;

import java.math.BigInteger;
import java.util.function.Function;

/**
 * You can modify the code in here, this will not be tested.
 */
public class Example {

	public static void main(String[] args) {
//		Function<BigInteger, BigInteger> fib = cachedRecursive(
//				(n, f) -> n.compareTo(BigInteger.TWO) <= 0 ? BigInteger.ONE
//						: f.apply(n.subtract(BigInteger.ONE)).add(f.apply(n.subtract(BigInteger.TWO))));
//		System.out.println(fib.apply(BigInteger.valueOf(1000L)));
	}
}
